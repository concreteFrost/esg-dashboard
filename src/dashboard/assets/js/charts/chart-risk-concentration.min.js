function getRandom() {
  var arr = [];

  while (arr.length < 7) {
    let rnd = Math.floor(Math.random() * 100) + 10;

    arr.push(rnd);
  }

  return arr;
}

var values = getRandom();
var values2 = getRandom();
var values3 = getRandom();

new Chart('chart-risk-concentration', {
  type: 'bar',
  data: {
    labels: [
      'United Kingdom',
      'UnitedStates',
      'India',
      'Italy',
      'Spain',
      'Germany',
      'Belgium',
    ],
    datasets: [
      {
        label:'Country',
        data: values,
        backgroundColor: '#03b1fc',
      },
      {
        label:'Business Unit',
        data: values2,
        backgroundColor: '#03c6fc',
      },
    ],
  },
  options: {
    legend: {
      display: 'true',
      position: 'right',
    },
    scales: {
      xAxes: [{ stacked: true }],
      yAxes: [
        {
          stacked: true,
          ticks: {
            beginAtZero: true,
          },
        },
      ],
    },

    plugins: {
      labels: {
        fontColor: '#567324',
        fontSize: 15,
        textMargin: -20,
        render: 'value',
      },
    },
  },
});
