$(document).ready(()=>{
    var title = localStorage.getItem('metric title')
    var score = localStorage.getItem('metric raw-score')
    var unit = localStorage.getItem('metric unit')
    $('.metric-title').html(title)
    $('.raw-score').html(score)
    $('.unit').html(unit)

})