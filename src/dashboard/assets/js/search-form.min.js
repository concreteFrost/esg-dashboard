$('#navbar-search-main').on('input', function () {
  const search = $('#navbar-search-main input').val();
  if (search.length > 1) {
    
    $(`*:contains(${search})`).css('color', 'red');
  }
});

$('#navbar-search-main').on('submit', function (e) {
  e.preventDefault();
});
