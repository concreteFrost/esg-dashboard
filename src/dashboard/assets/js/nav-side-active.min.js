var current;

$(document).ready(function () {
  $('a').removeClass('active');
  changeSectionOnNav();
  console.log(window.location.href.split("#")[1])
});

window.addEventListener('hashchange', () => {
  $(current).css('backgroundColor', '#ffffff');
  changeSectionOnNav();
});

function changeSectionOnNav() {
  $('.navbar-inner li a').each(function () {
    if ($(this).prop('href') === window.location.href && window.location.href.split("#")[1].length>0) {
        current = $(this);
        $(this).css('backgroundColor', '#bbe4fc');
        $(this).parents('div.collapse').addClass('show');
        $(this)
          .parents('.expandable')
          .find('a:first')
          .attr('aria-expanded', 'true')
          .addClass('active');
      } 
    
  });
}
