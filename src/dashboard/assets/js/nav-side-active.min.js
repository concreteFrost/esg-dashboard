

$(document).ready(function () {
  var url = location.pathname;
  const shorten = url.replace('/dashboard/', '../');
  $('.navbar-inner li a').each(function () {
    if ($(this).prop('href') == window.location.href ||$(this).prop('href') == window.location.href.substr(1)) {
        $(this).css('backgroundColor','#c2fbff');
    }
});
});

function highlightMain(url) {

  // if (url.includes('company') || url.includes('metrics')) {
  //   highlightSub(url,'#metrics','#company');
    
  // }
  // if (url.includes('frameworks')) {
  //   highlightSub(url,'#frameworks-nav','#frameworks');
  // }
  // if (url.includes('calendar')) {
  //   $('#calendar-nav').addClass('active');
  // }
  // if (url.includes('reports')) {
  //   $('#reports-nav').addClass('active');
  // }
}

function highlightSub(url,id,parentID) {
  // $(`ul a[href="${url}"]`).css({ backgroundColor: '#ff0000'});
  // $(id).addClass('active');
  // $(id).addClass('show');
  // $(id).closest('[aria-expanded]').attr('aria-expanded','true');
  // $(parentID).addClass('show')
  // $(parentID).attr('aria-expanded','true');

}
